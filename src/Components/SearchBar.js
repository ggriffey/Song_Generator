import { React } from "react";

const SearchBar = () => {
  return (
    <div className="searchbar">
      <input type="text" placeholder="search for an artist or song"></input>
    </div>
  );
};

export default SearchBar;
